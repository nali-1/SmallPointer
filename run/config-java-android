DIR0=$(cd -- $(dirname -- $0) && pwd)
cd $DIR0/..
run/config
F=CMakeLists.txt
sed -i '/#set(ST java_android)/c\set(ST java_android)' $F
sed -i '/#set(GL 1)/c\set(GL 1)' $F
sed -i '/#set(CLIENT 1)/c\set(CLIENT 1)' $F

while true; do
	echo '1)DEBUG'
	echo '2)NONE'
	read i
	if [ $i = 1 ]; then
		sed -i '/#set(DEBUG 1)/c\set(DEBUG 1)' $F
		break
	elif [ $i = 2 ]; then
		break
	fi
done

while true; do
	echo '1)GL_DEBUG'
	echo '2)NONE'
	read i
	if [ $i = 1 ]; then
		sed -i '/#  set(GL_DEBUG 1)/c\  set(GL_DEBUG 1)' $F
		break
	elif [ $i = 2 ]; then
		break
	fi
done
